import { Markdown } from '@storybook/addon-docs/blocks'
import {
    Canvas,
    ColorItem,
    ColorPalette,
    Meta,
    Story
} from '@storybook/addon-docs/blocks'

import { colors } from '@nashtech-garage/stylesheet/integrations/tailwind'

<Meta title="Foundation/Colors/Danger" />

<ColorPalette>
    {[
        {
            name: 'danger-bold',
            value: colors.danger.bold
        },
        {
            name: 'danger-regular',
            subtitle: '(default)',
            value: colors.danger.DEFAULT
        },
        {
            name: 'danger-light',
            value: colors.danger.light
        },
        {
            name: 'danger-thin',
            value: colors.danger.thin
        }
    ].map((color) => (
        <ColorItem
            key={color.name}
            title={color.name}
            subtitle={color?.subtitle}
            colors={{ [color.name]: color.value }}
        />
    ))}
</ColorPalette>
