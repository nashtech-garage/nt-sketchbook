import { Markdown } from '@storybook/blocks'
import {
    Canvas,
    ColorItem,
    ColorPalette,
    Meta,
    Story,
} from '@storybook/blocks'

import { colors } from '../../../../nt-stylesheet/themes/colors.ts'

<Meta title="Colors/Warning" />

<ColorPalette>
    {[
        {
            name: 'bold',
            value: colors.warning.bold,
        },
        {
            name: 'regular (default)',
            value: colors.warning.DEFAULT,
        },
        {
            name: 'light',
            value: colors.warning.light,
        },
        {
            name: 'thin',
            value: colors.warning.thin,
        },
    ].map((color) => (
        <ColorItem
            key={color.title}
            title={color.name}
            colors={{ [color.name]: color.value }}
        />
    ))}
</ColorPalette>
