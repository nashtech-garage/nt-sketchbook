import { Markdown } from '@storybook/addon-docs/blocks'
import {
    Canvas,
    ColorItem,
    ColorPalette,
    Meta,
    Story
} from '@storybook/addon-docs/blocks'

import { colors } from '@nashtech-garage/stylesheet/integrations/tailwind'

<Meta title="Foundation/Colors/Info" />

<ColorPalette>
    {[
        {
            name: 'info-bold',
            value: colors.info.bold
        },
        {
            name: 'info-regular',
            subtitle: '(default)',
            value: colors.info.DEFAULT
        },
        {
            name: 'info-light',
            value: colors.info.light
        },
        {
            name: 'info-thin',
            value: colors.info.thin
        }
    ].map((color) => (
        <ColorItem
            key={color.title}
            title={color.name}
            subtitle={color?.subtitle}
            colors={{ [color.name]: color.value }}
        />
    ))}
</ColorPalette>
