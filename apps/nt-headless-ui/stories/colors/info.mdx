import { Markdown } from '@storybook/blocks'
import {
    Canvas,
    ColorItem,
    ColorPalette,
    Meta,
    Story,
} from '@storybook/blocks'

import { colors } from '@stylesheet/themes/colors.ts'

<Meta title="Colors/Info" />

<ColorPalette>
    {[
        {
            name: 'bold',
            value: colors.info.bold,
        },
        {
            name: 'regular (default)',
            value: colors.info.DEFAULT,
        },
        {
            name: 'light',
            value: colors.info.light,
        },
        {
            name: 'thin',
            value: colors.info.thin,
        },
    ].map((color) => (
        <ColorItem
            key={color.title}
            title={color.name}
            colors={{ [color.name]: color.value }}
        />
    ))}
</ColorPalette>
