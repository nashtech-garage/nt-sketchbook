{
    "name": "nt-stylesheet",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "targets": {
        "build": {
            "executor": "@nx/vite:build",
            "outputs": [
                "{options.outputPath}"
            ],
            "options": {
                "configFile": "apps/nt-stylesheet/vite.config.ts",
                "outputPath": "apps/nt-stylesheet/dist"
            }
        }
    }
}
